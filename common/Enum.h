#pragma once
enum CameraType { LOCK, FREE, SELECT }; //SELECT is not implemented yet

enum RenderModes {
    GRAPHICS = 0,
    COMPUTE,
    COMPUTE_SWAPCHAIN,
    COMPUTE_GRAPHICS,
    GRAPHICS_SHADOWMAP
};


